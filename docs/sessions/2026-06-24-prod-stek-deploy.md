# 2026-06-24 — Документирование прод-стека деплоя

## Что сделано
Зафиксирован в документации целевой продакшен-стек, которого пока нет в каркасе. Только документация, без кода/конфигов/Docker.

- `docs/architecture.md` — добавлен раздел **§9 «Веб-сервер и деплой (production)»**:
  - dev-серверы (Django runserver:8000, Next.js dev:3000) — только для разработки, не для прода;
  - backend: реверс-прокси → Gunicorn (WSGI, несколько воркеров) → Django;
  - реверс-прокси/HTTPS — Coolify (Traefik/Caddy внутри), маршрутизация `/api` → backend, остальное → frontend;
  - frontend: `next build` + `next start` вместо `next dev`;
  - статика (`collectstatic`) и медиа — через прокси либо напрямую из MinIO;
  - прод-настройки Django: `DEBUG=0`, корректный `ALLOWED_HOSTS`, секреты только из env.
- `docs/progress.md` — добавлен раздел **«Волна деплоя / продакшен — статус: не начато»** с чеклистом из 5 пунктов, ссылающимся на §9.

## Следующий шаг
Без изменений по сути — двигаться по Волне 1 (PRODUCT_SPEC.md), начиная с 1.1 (регистрация и роли). Волна деплоя — отложенная задача, к ней возвращаться перед первым реальным деплоем на VM.
